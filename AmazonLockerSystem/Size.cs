namespace AmazonLockerSystem
{
    /// <summary>
    /// Size of the locker.
    /// Leave sufficient range between the size, so that it is scalable in the feature if in between size comes up.
    /// </summary>
    public enum Size
    {
        Small = 10,
        Medium = 100,
        Large = 200
    }
}
